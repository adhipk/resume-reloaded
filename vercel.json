{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "framework": null,
    "installCommand": "bun install && mkdir -p .vercel-tools && curl -fsSL https://github.com/jgm/pandoc/releases/download/3.8.3/pandoc-3.8.3-linux-amd64.tar.gz | tar -xz -C .vercel-tools && python3 -m venv .vercel-tools/venv && .vercel-tools/venv/bin/pip install weasyprint==67.0",
    "buildCommand": "PATH=\"$PWD/.vercel-tools/pandoc-3.8.3/bin:$PWD/.vercel-tools/venv/bin:$PATH\" bun run build",
    "outputDirectory": "out"
}